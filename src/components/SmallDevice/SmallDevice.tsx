import { FC } from "react";

const SmallDevice: FC = () => {
  return (
    <div className="max-w-7xl mx-auto mt-20 px-4 text-secondary text-center text-3xl">
      Sorry, the website doesn't work on small devices.
    </div>
  );
};

export default SmallDevice;
